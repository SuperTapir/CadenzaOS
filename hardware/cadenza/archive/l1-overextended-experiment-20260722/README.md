# L1 过度扩展实验归档

归档日期：2026-07-22

此目录只读保存此前已经做到一半、但超出最新 L1 范围的实验候选。该候选混入了 USB 移位、独立 Power/Lock、额外测试点和相关布线，不再作为 Cadenza L1 的母版，也不代表可生产设计。

归档内容包括：

- L1 原理图、PCB 同步、布局和局部布线候选；
- 当时的 DRC、ERC、门禁输出和分析报告；
- 当时的屏幕外壳 fit-check；
- Power/Lock 相关选型资料；
- 修改前的 OpenSpec 规划快照。

`SHA256SUMS` 覆盖归档时的全部原文件。新增本说明后，原文件校验仍可用；本说明本身不在清单内。

新的 L1 必须从 `hardware/cadenza/golden-import/` 派生，并只修改 Sharp LS027B7DH01 显示子系统和对应屏幕安装结构。
