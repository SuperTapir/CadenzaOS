## 1. 受约束布局契约与失败用例

- [x] 1.1 增加可移植测试，覆盖 request 校验、首选倍数恰好容纳、选择最大可用倍数、对齐、结果边界报告和 framebuffer guard pixels。
- [x] 1.2 增加失败测试，覆盖 `ellipsis`、达到行数上限的 `wrap`、确定性的 marquee phase，以及预期裁剪不产生误用诊断。
- [x] 1.3 定义固定容量的受约束文本 request、result、status、policy 和行片段类型，并记录半开矩形及生命周期语义。

## 2. 可移植布局与光栅化

- [x] 2.1 使用现有 `MonoCanvas` 字体度量和整数倍数，实现确定性的 `fit` 选择。
- [x] 2.2 实现无分配的 ASCII `ellipsis` 和 `wrap` 解析，覆盖空白断行、过长 token 硬拆、最大行数和最后一行截断。
- [x] 2.3 实现由 phase 驱动且不访问墙上时钟的 marquee 解析。
- [x] 2.4 实现受约束光栅化：限制所有像素、保留底层误用诊断，并且只抑制有效 result 已声明的裁剪。
- [x] 2.5 运行聚焦的 canvas/layout 测试，并补充单像素区域、无省略号容量、极端坐标和重复确定性等边界用例。

## 3. Launcher 迁移

- [x] 3.1 增加回归用例，证明 320×170 下 `Animation Gallery` 完整位于 Launcher 卡片内，且异常超长的主/次标签都不会离开各自区域。
- [x] 3.2 将固定倍数的 Launcher 动态标题和 footer 绘制替换为按语义角色定义的受约束 request，并显式使用 `fit` 到 `ellipsis` 的策略。
- [x] 3.3 验证 Launcher 标签不会产生意外的 text-clipped 诊断；完成视觉审查后，只更新受影响的 320×170 与 400×240 快照 hash。

## 4. 其他菜单消费者与回归验证

- [x] 4.1 盘点菜单式 UI 中接收运行时文本的 `canvas.text` 调用，记录每个调用的视觉容器、邻接元素和 overflow policy。
- [x] 4.2 将盘点中确实可能接收可变长度内容且缺少区域约束的调用迁移到受约束文本 API；静态常量标签保持不变。
- [x] 4.3 增加 320×170 与 400×240 下的菜单边界回归测试，断言动态文本不越过所属区域、不侵占相邻元素且不产生意外 text-clipped 诊断。

## 5. 端到端验证与文档

- [x] 5.1 运行完整 host 测试、快照测试、desktop smoke 测试，以及轻量格式和 diff 检查。
- [x] 5.2 在 320×170 与 400×240 下启动真实 SDL 模拟器，遍历 Launcher 的所有选项，并保存主标题、左右相邻标签和导航指示器均互不侵占的视觉证据。
- [x] 5.3 记录受约束文本 API、policy 选择指引、固定容量上限，以及预期溢出与误用诊断之间的区别。
