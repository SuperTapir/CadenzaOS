## 1. 参考基线与审查证据

- [x] 1.1 保存嘉立创 V2/V3 原工程、页面、图片、附件和固件镜像，并记录原文件哈希。
- [x] 1.2 从 V2 原工程确认原理图、PCB、符号、封装和器件属性可编辑，并提取组件与网络基础统计。
- [x] 1.3 完成参考工程只读评估报告，列出继承、修改、风险、验证和缺失资料，并区分适配阻断、首板观察和可选优化。
- [x] 1.4 记录 GPL-3.0、平台转载设置、硬件源文件和仅二进制固件的复用边界与发布义务。
- [x] 1.5 对全部关键器件建立当前库存、生命周期、封装、嘉立创基础/扩展属性和替代料快照。

## 2. EasyEDA Pro 到 KiCad 转换门禁

- [x] 2.1 从原 `.esch/.epcb` 生成机器可读的组件、网络、层、过孔、安装孔、板框、关键坐标和铺铜基线清单。
- [x] 2.2 将冻结的 V2 `.epro` 导入新的 KiCad 派生目录，保存原始导入结果且不做功能修改。
- [x] 2.3 对原工程与 KiCad 导入结果执行逐项差异审计，并渲染原理图、PCB 和 3D 视图进行人工对照。
- [x] 2.4 根据差异审计决定继续使用 KiCad 主工程或回退嘉立创 EDA 直接修改，并保存 golden 工程与结论。
- [ ] 2.5 将通过门禁的派生工程重新导入嘉立创 EDA，验证往返转换后的网络、板框、孔、封装和铜皮。

## 3. L1 屏幕电气适配

- [ ] 3.1 用屏幕实物和已购转接板确认 FPC Pin 1、触点面、上接/下接、自然出线方向和十个引脚连续性。2026-07-22 用户正反面照片已确认显示面朝上时 FPC 从 6 点钟出线、金手指位于屏幕背面；照片没有无歧义 Pin 1 标记，转接板 1-10 连续性与折后全局 Pin 1 仍待测。
- [ ] 3.2 在 ESP32-S3-DevKitC-1 上逐线验证 LS027B7DH01 的 5 V 供电、3.3 V 信号、初始化、全屏图案、EXTCOMIN 和静态保持，并记录电流与异常恢复。
- [x] 3.3 锁定可采购的 10-pin 0.5 mm FPC 座、封装、可合法用于工程的保守 3D 包络、LCSC 料号和至少一个替代料；L1 主料为 `FH34SRJ-10S-0.5SH(50)` / `C324723`，替代为方向确认后的 Hirose FH12/FH12A。Hirose 官网 STEP 因禁止分发、修改和商业使用而不纳入工程。
- [x] 3.4 从与 golden PCB 逐字节一致、仅修复 KiCad 导入语义的 working-base 重新建立干净 L1：删除 ST7789 FPC、背光和显示 LDO，优先原位复用 C20/C21 作为 Sharp 的 5 V 去耦，仅新增 LS027B7DH01、DISP 电容与 EXTMODE 固定连接；不得带入旧候选的 USB、Power/Lock、按键或无关测试点修改。
- [x] 3.5 冻结 L1 GPIO 映射为 `SCLK=GPIO39`、`SI=GPIO48`、`SCS=GPIO47`、`EXTCOMIN=GPIO14`、`DISP=GPIO12`；静态端点检查确认没有重新分配 microSD、音频、现有按键或 CH340C USB 路线。启动绑带、时序和波形仍列入 3.7/首板实测。
- [x] 3.6 保持参考功能区锁定，仅在显示区域完成器件替换、局部布局和必要重布线；屏幕/窗口采用 `X +1.5 mm`、KiCad `Y -1.5 mm` 偏移，`J_DISP1=(154.625 mm, 128.500 mm), 0°`。`USB1`、`U4`、板框和孔不动，USB `D+`、`D-`、`CC1` 仅局部绕行且端点不变；`C20/C21` 作为显示去耦允许移动。
- [ ] 3.7 完成 L1 ERC、DRC、人工引脚/封装/供电时序复审和显示连接器方向复审。已完成部分：原理图候选 20/20、PCB 边界 25/25、DRC 从参考 192 项降至候选 95 项、0 unconnected、0 shorting_items、Sharp 局部无新增问题；正反面实物照片已确认 FPC 出线和触点面。未完成部分：真实 FPC Pin 1 独立标记/连续性、折弯、插入、锁扣、合壳及实测供电时序。
- [x] 3.8 比较 L1 与参考工程的电源、USB、现有按键、音频和存储区域；70 个冻结器件的物理几何、USB 焊盘网络、U4、板框和孔通过边界门禁。仅记录 `U4.41/U7.3` EasyEDA 分裂焊盘语义归一、显示去耦移动和 USB 局部铜线绕行，不把这些导入/显示直接影响误报为功能区重设计。

## 4. L1 外壳与屏幕 fit-check

- [x] 4.1 提取参考顶盖/底盒 STEP 的外形、螺柱、PCB 支撑、接口、扬声器、电池和屏幕窗口基准。
- [x] 4.2 建立 LS027B7DH01 玻璃、可视区、FPC、弯折区和连接器的机械模型与共享坐标；用户正反面照片确认实物 FPC 出线和触点面符合模型，折后路径仍由 4.4 实装关闭。
- [x] 4.3 生成保留参考底盒和固定关系的 L1 前壳 STEP/STL，缩放屏幕窗口并增加软垫代理、独立压框和 FPC 过线槽；正式固定/预压方式仍由 4.4 实装决定。
- [ ] 4.4 输出 L1 fit-check 打印文件和装配说明，检查屏幕无应力、窗口无遮挡、FPC不受拉力和外壳可闭合。
- [ ] 4.5 将 L1 外壳与 PCB STEP 组合，完成安装孔、屏幕、USB、开关、电池和扬声器硬碰撞检查。
- [ ] 4.6 比较 L1 与参考 STEP，证明电源开关、USB、按键、扬声器、底盒、螺柱和 PCB 固定关系保持不变，外壳差异仅限屏幕安装区域。

## 5. L2 输入与 USB 决策

- [ ] 5.1 根据已购 EC12 实物锁定轴型、轴长、定位脚、脉冲/卡点、按压行程、寿命、封装和可采购料号。
- [ ] 5.2 冻结左侧 `B`/`Menu` 与右侧编码器/中心 `A` 的中心坐标、键帽包络和拇指可达范围。
- [ ] 5.3 在原理图中删除摇杆和多余游戏键，新增编码器 A/B相、中心 A、独立 B/Menu、必要上拉/去抖和测试点。
- [ ] 5.4 仅在 L2 确有产品需要时评估 ESP32-S3 原生 USB；默认继续使用 L1 已验证的 CH340C，不得将两个 USB PHY 直接并联。
- [ ] 5.5 完成 L2 PCB 局部布局与布线，保留参考电源、音频、存储和安装基准，确保编码器机械力不由焊盘独立承担。
- [ ] 5.6 生成参数化 B/Menu 键帽、编码器旋钮和中心 A 视觉区域的 STEP/STL，并记录打印间隙和方向。
- [ ] 5.7 修改前壳输入开孔、轴套和承力结构，完成旋转、按压、误触、键程、握持和 PCB/外壳碰撞检查。
- [ ] 5.8 完成板级输入映射，使旋转、A、B、Menu 分别对应 Navigate、Confirm/Primary、Back、Menu；另行判断 Power/Lock 是否能以简单可靠方式加入，不能则明确省略。

## 6. L3 整机优化

- [ ] 6.1 在 L1/L2 装配通过后优化外壳比例、圆角、握持边、屏幕留白、键帽层级和旋钮纹理。
- [ ] 6.2 优化扬声器格栅、音腔密封、接口开孔、螺柱、装配顺序和维修可达性。
- [ ] 6.3 核对天线净空、电池膨胀余量、FPC保护、开关防误触和桌面放置稳定性。
- [ ] 6.4 打印 L3 外壳与全部独立零件，记录尺寸偏差、装配照片、手感和必须回改项。

## 7. 生产与首板门禁

- [ ] 7.1 为最终装配版本完成关键数据手册引脚、典型电路、绝对最大值、热焊盘和封装核对。
- [ ] 7.2 运行 KiCad 原理图/PCB/热分析、适用的 SPICE、EMC预一致性和人工复核，并对工具告警做误报分诊。
- [ ] 7.3 完成 BOM 生命周期、库存、价格、基础/扩展料、替代料、DNP和人工装配审计。
- [ ] 7.4 导出 Gerber、钻孔、BOM、CPL、装配图、坐标和制造说明，并验证 BOM/CPL designator 集合与极性旋转。已完成部分：临时制造包可重复生成，70 个 BOM/CPL 位号完全一致且唯一，四个 M2 孔为 2.0 mm NPTH，JLC 保守几何门禁通过；未完成部分：嘉立创上传后的元件匹配与逐个极性/旋转预览。
- [ ] 7.5 在嘉立创 EDA 专业版回导并完成 ERC/DRC、3D 装配、元件匹配和生产预览；记录所有与 KiCad 的差异。
- [ ] 7.6 L1 输出前壳、后壳和屏幕压框 STEP/STL；L2 再输出旋钮与键帽，运行网格封闭、尺寸和装配碰撞检查。
- [ ] 7.7 生成首板下单包与最终审查报告，明确可直接下单范围、待实物验证项和非量产冻结声明。
- [ ] 7.8 制作少量 L1 工程样机，按参考电源操作、刷写、Sharp 屏幕、原有输入、存储、扬声器、无线、续航和装配顺序验收；Power/Lock 只在 L2 实际纳入时验证。
